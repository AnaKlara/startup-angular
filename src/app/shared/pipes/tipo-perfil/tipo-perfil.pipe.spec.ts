import { TipoPerfilPipe } from './tipo-perfil.pipe';

describe('TipoPerfilPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPerfilPipe();
    expect(pipe).toBeTruthy();
  });
});
